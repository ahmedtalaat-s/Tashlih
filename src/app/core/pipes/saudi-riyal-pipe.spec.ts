import { SaudiRiyalPipe } from './saudi-riyal-pipe';

describe('SaudiRiyalPipe', () => {
  it('create an instance', () => {
    const pipe = new SaudiRiyalPipe();
    expect(pipe).toBeTruthy();
  });
});
